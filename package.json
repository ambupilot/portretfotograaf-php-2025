{
  "name": "portret-fotograaf",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./css/input.css -o ./css/main.css --minify",
    "watch": "npx tailwindcss -i ./css/input.css -o ./css/main.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0"
  }
}
